/*
Used for storing render data into a gbuffer which can be used later to calcuate what is needed for raytraced lighting.
*/
#define VERTEX_SHADER
#define USE_VELOCITY_OFFSET
#define WRITE_VELOCITY

#include "gbuffer.inc"
